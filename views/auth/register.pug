extends /__layouts/base.pug

block vars
  - var title = "Inscription"

block main
  if registered
    p Votre compte a bien été créé. Veuillez suivre le lien envoyé à #[strong=email] pour l'activer.

  else
    form(method="POST" action="/auth/inscription")
      input(type="hidden" name="_csrf" value=csrf_token)
      div.form-group
        label(for="username") Nom d'utilisateur 
        input(type="text" id="username" name="username" value=user.username required="")
      div.form-group
        label(for="email") Adresse électronique 
        input(type="email" id="email" name="email" value=user.email required="")
      div.form-group
        label(for="plain_password") Mot de passe
        input(type="password" id="plain_password" name="plain_password" required="")
      div.form-group
        label(for="confirm_password") Confirmer le mot de passe
        input(type="password" id="confirm_password" name="confirm_password" required="")
      div.form-submit
        button(type="submit") S'inscrire
    
    p Déjà un compte ? #[a(href="/auth/connexion") Se connecter]